// import ChatEvent from "components";
import { ChatEventGroup as ChatEventGroupType } from "types";

const ChatEventGroup = ({ timestamp, chatEvents }: ChatEventGroupType) => {
    // const Events = chatEvents.map(chatEvent => <ChatEvent {...chatEvent} />)
    // return (
    //     <div>
    //         <div>{chatEventGroup.timestamp}</div>
    //     </div>
    // );

    return (<div></div>);
}

export default ChatEventGroup;